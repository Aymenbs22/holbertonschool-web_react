export default function filterTypeSelected(event){
    return event.filter;
}

export default function getNotifications(list){
    return (Map(list.filter));
}

export default function getUnreadNotifications(list){
    return (Map(list.filter));
}